# Eén workspace voor de drie crates van de app, naar het patroon van
# spike/Cargo.toml (bevinding B18): een apart workspace per project, met één
# gecommitte lock, zodat de build reproduceerbaar is.
[workspace]
resolver = "2"
members = ["vault-core", "app-state", "src-tauri"]
