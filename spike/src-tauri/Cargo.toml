[package]
name = "lapis-spike"
version = "0.0.0"
edition = "2021"
publish = false

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }

# Alle bestandslogica zit hier. Deze crate is een dunne schil.
vault-core = { path = "../vault-core" }
